apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prometheus
  namespace: argocd
spec:
  project: playground
  sources:
    - repoURL: https://github.com/scofe97/Redpanda-PlayGround.git
      targetRevision: main
      ref: values
    - repoURL: https://prometheus-community.github.io/helm-charts
      chart: prometheus
      targetRevision: "*"
      helm:
        valueFiles:
          - $values/infra/k8s/monitoring/prometheus/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: rp-mgm
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
