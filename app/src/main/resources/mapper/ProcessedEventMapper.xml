<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.playground.common.idempotency.ProcessedEventMapper">

    <select id="existsByCorrelationIdAndEventType" resultType="boolean">
        SELECT EXISTS(
            SELECT 1 FROM processed_event
            WHERE correlation_id = #{correlationId} AND event_type = #{eventType}
        )
    </select>

    <insert id="insert" parameterType="com.study.playground.common.idempotency.ProcessedEvent">
        INSERT INTO processed_event (correlation_id, event_type, processed_at)
        VALUES (#{correlationId}, #{eventType}, NOW())
        ON CONFLICT (correlation_id, event_type) DO NOTHING
    </insert>

</mapper>
