<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.playground.connector.mapper.ConnectorConfigMapper">

    <select id="findAll" resultType="com.study.playground.connector.domain.ConnectorConfig">
        SELECT id, stream_id, tool_id, yaml_config, direction, created_at
        FROM connector_config
        ORDER BY id
    </select>

    <select id="findByToolId" resultType="com.study.playground.connector.domain.ConnectorConfig">
        SELECT id, stream_id, tool_id, yaml_config, direction, created_at
        FROM connector_config
        WHERE tool_id = #{toolId}
    </select>

    <insert id="insert" parameterType="com.study.playground.connector.domain.ConnectorConfig"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO connector_config (stream_id, tool_id, yaml_config, direction, created_at)
        VALUES (#{streamId}, #{toolId}, #{yamlConfig}, #{direction}, NOW())
    </insert>

    <delete id="deleteByToolId">
        DELETE FROM connector_config WHERE tool_id = #{toolId}
    </delete>

</mapper>
