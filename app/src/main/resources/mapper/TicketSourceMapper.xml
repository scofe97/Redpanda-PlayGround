<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.playground.ticket.mapper.TicketSourceMapper">

    <select id="findByTicketId" resultType="com.study.playground.ticket.domain.TicketSource">
        SELECT id, ticket_id, source_type, repo_url, branch, artifact_coordinate, image_name, created_at
        FROM ticket_source
        WHERE ticket_id = #{ticketId}
    </select>

    <insert id="insertBatch">
        INSERT INTO ticket_source (ticket_id, source_type, repo_url, branch, artifact_coordinate, image_name, created_at)
        VALUES
        <foreach collection="sources" item="s" separator=",">
            (#{ticketId}, #{s.sourceType}, #{s.repoUrl}, #{s.branch}, #{s.artifactCoordinate}, #{s.imageName}, NOW())
        </foreach>
    </insert>

    <delete id="deleteByTicketId">
        DELETE FROM ticket_source WHERE ticket_id = #{ticketId}
    </delete>

</mapper>
