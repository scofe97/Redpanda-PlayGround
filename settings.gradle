pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url 'https://packages.confluent.io/maven/' }
    }
}

rootProject.name = 'redpanda-playground'

include 'common-kafka', 'app'
